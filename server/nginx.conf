server {
  listen 80 default;
  server_name _;
  gzip on;
  charset utf-8;

  root /usr/share/nginx/html;

  location /rss {
    return 301 /index.xml;
  }

  location / {
    error_page 404 /404.html;
  }
}
